#pragma once

// Zeigt die Uhrzeit als Wortuhr an
void showTime(int hour, int minute);

// Zeigt alle Wörter für LED-Test (nacheinander mit Pausen)
void showAllWords();

// Zeigt nur die erste LED für Test
void showFirstLED();

// Test: Zeigt alle Wörter gleichzeitig
void showAllWordsAtOnce();
